# Copyright 2009 Jonathan Dahan <jedahan@gmail.com>
# Distributed under the terms of the GNU General Public License v2

require sourceforge [ project=incrtcl pnv=${PN}${PV}b1 suffix=tar.gz ]

SUMMARY="Object Oriented Enhancements for Tcl/Tk"

SLOT="0"
LICENCES="BSD"
PLATFORMS=""

DEPENDENCIES="dev-lang/tcl"

WORK="${WORKBASE}/${PN}${PV}"

DEFAULT_SRC_PREPARE_PATCHES=( "${FILES}"/configure.patch )
DEFAULT_SRC_CONFIGURE_PARAMS=( --hates=docdir )

src_install() {
    default
    rmdir "${IMAGE}"/usr/bin
}

